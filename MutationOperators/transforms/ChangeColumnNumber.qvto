import m2m.qvt.oml.ExampleJavaLib;

modeltype PIM uses MSWord2007('http://benchmarkdp.documentgenerator.PIM');
	

transformation AddPage(inout model:PIM);

main() {
	var doc:= model.objectsOfType(Document); 
	doc->map ChangeColumnNumber();
}


mapping inout Document::ChangeColumnNumber() {
	numColum := getRandom(3)
}